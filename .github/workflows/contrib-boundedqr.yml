name: Companion boundedqr
on:
  pull_request:
    paths: ["contrib/boundedqr/**", ".github/workflows/contrib-boundedqr.yml", "econhdfe/**", "pyreghdfe/**"]
  push:
    branches: [main]
    paths: ["contrib/boundedqr/**", ".github/workflows/contrib-boundedqr.yml", "econhdfe/**", "pyreghdfe/**"]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  cpu:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.10", "3.12"]
    env:
      OPENBLAS_NUM_THREADS: "1"
      OMP_NUM_THREADS: "1"
      NUMBA_NUM_THREADS: "4"
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python }}
      - run: python -m pip install . "./contrib/boundedqr[test]" build
      - run: python -m pytest -q
        working-directory: contrib/boundedqr
      - run: python -m build
        working-directory: contrib/boundedqr
      - run: python examples/clustered_quantiles.py
        working-directory: contrib/boundedqr
