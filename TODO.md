# econhdfe 统一 TODO / ROADMAP

基线：0.6.3 本地候选版。更新：2026-09-14。
性质：本文件是源码内统一任务入口；原型、实施、本地验收与跨环境验收分开记录。没有创建远程 issue。目标版本由维护者按版本政策决定；本表不承诺日期。Owner 为建议责任角色，不是已分配的真实人员。

## 发布前维护增量（以本节覆盖旧状态描述）

见 `RELEASE_CLOSEOUT.md`。CI-01（Enum 反射）、CI-02（换行）、CI-03
（workqueue 并发崩溃）已有修复与回归测试；原 12 格 CI 增加 4 格最低运行依赖。
REL-01/02 必须对修复后的最终提交重验，旧本地通过不能直接迁移；REL-04
仍需提交与制品的外置哈希绑定。本轮不扩展 P1/P2 功能，不提前发布。

## 状态与放行规则

发布 P0、优化 P1、长期 P2 分开管理。`已评审` 是过程状态，不等于 `验收通过`。`docs/release/execution.json` 已在 maintenance 审阅表之外记录 execution_status、环境、运行/验证代码指纹和带 SHA256 的证据。正式制品与真实提交的绑定使用外置执行记录；参见 `docs/release/acceptance.md`。

状态使用：待实施 / 原型已验证 / 待验收 / 条件阻塞 / 需求待确认 / 已完成。blocked 必须记录缺少的环境或依赖；不以 reviewed 消除 blocked。所有实际 Owner 目前待指定。

## A. 正式发布门禁（不依赖先实现全部优化）

| ID | 优先级 | 当前状态 | 工作与依赖 | 验收标准 | 建议责任角色 |
|---|---|---|---|---|---|
| REL-01 | P0 | 条件阻塞 | 严格 sdist→wheel 隔离构建及全新环境安装；需要完整依赖索引或 wheelhouse | 不继承宿主 site-packages；最终 wheel 在源码目录外完整安装/运行；记录依赖版本和原始日志；不能使用离线宿主模式替代 | 发布工程 |
| REL-02 | P0 | 待验收 | 执行声明的 Python/OS CI 矩阵；补各 Python 可行的最低依赖组合 | 真实执行结果与支持声明一一对应；失败修复或收窄声明；保留测试数量、跳过项、平台架构、依赖和制品哈希 | 发布工程 |
| REL-03 | P0 | 本地实施验收完成 | 独立执行门禁已落地；已审阅/条件阻塞/篡改证据不能通过正式模式。REL-01/02 等实际结果仍待取得 | reviewer=reviewed 但 execution=blocked 的项目，不能通过正式发布门禁；候选包可明确标记且允许保留阻塞 | 发布工程 |
| REL-04 | P0 | 待验收 | 冻结实际提交、迁移说明、版本、源码/制品哈希和真实项目入口 | 最终提交与 CI、文档、wheel/sdist/ZIP 对应；旧版受影响计算需重算的说明保留；不虚构联系邮箱或远程运行结果 | 维护者 |
| VAL-01 | P0（按声明） | 待验收 | 外部兼容性主张与可执行对照集对应；涉及 Stata 的项目需要合法参考环境 | 模型、样本、权重、DoF/修正、参考版本一致；差异分类；没有证据的主张收窄，而非阻塞未作该主张的普通 GitHub 发行 | 统计验证 |

## B. 小改动性能路线

| ID | 优先级 | 当前状态 | 位置/范围 | 验收标准 | 建议责任角色 |
|---|---|---|---|---|---|
| PERF-01 | P1，首选 | 已合入并本地验证；跨平台待验收 | `HDFEAbsorber._weighted_colsum` CPU einsum 融合 | 小 PR，不改公式/容差；807 项原有回归+52 项布局/空矩阵/动态权重/极端尺度/近停机阈值守卫；独立进程、多种规模重测；GPU 分支不改 | 数值核心 |
| PERF-02 | P1，次选 | 原型已验证，收益不一致 | `_sym_projection` 的私有 z 输出复用 | 与 PERF-01 分开测；输入不被修改，无别名污染，C/F/sliced 输入一致；隔离分配/峰值内存有记录；没有稳健收益可暂不合入 | 数值核心 |
| PERF-03 | P1 | 待测量 | `stable_linalg.py` 的缩放写入缓冲区和分块求尺度 | QR/SVD、有效秩与估计/协方差一致性保留；量纲/秩亏/宽窄矩阵测试通过；证明减少峰值分配且常见小样本无实质回退 | 数值核心 |
| PERF-04 | P1 | 待测量 | 单次拟合聚类编码与交互组合映射复用 | 先 profile 确认成本；最终样本/缺失/标签变化正确失效；字符串、巨大整数、单聚类拒绝、多向交互完全保留 | 推断/数据层 |
| PERF-05 | P1 | 待验收 | 现有 fused/indexed、RHS 分块、线程与内存预算校准 | 分别记录冷启动/热运行、真实路由、总耗时/分阶段耗时/峰值 RSS；多规模和不平衡 FE；不只在单主机上改所有默认值 | 执行规划 |
| PERF-06 | P1 | 待验收 | 用好已实现的 session / PPML warm-start / 拓扑缓存 | 以典型多规格工作流验证缓存收益；修改样本/权重/变量值均不产生旧结果；不跨 IRLS 权重复用过期数值投影 | 工作流/数据层 |

## C. 稳健性和证据补齐

| ID | 优先级 | 当前状态 | 工作 | 验收标准 | 建议责任角色 |
|---|---|---|---|---|---|
| QA-01 | P1 | 已实施并本地验证 | `scripts/run_tests.py` 将功能测试线程前提与独立单线程性能进程分开 | 当前单线程 cap 导致的 4 项线程测试失败有明确环境要求；正常多线程断言保留；受限环境行为另测，不把功能缺测当通过 | 测试工程 |
| QA-02 | P1 | 待扩展 | 精确秩 native/有理回退的统一资源边界 | 记录 max work/内存/终止原因；预算或资源不足显式失败或选择已证明安全的策略；不能返回模秩下界冒充 exact | HDFE/数学 |
| QA-03 | P1 | 待扩展 | 优先补低覆盖高风险失败分支，而非追逐总覆盖率数字 | 数值非有限、收敛边界、缓存失效、坏素数、Gram 信息损失路径均有可复现守卫；实际分支覆盖与跳过项自动生成 | 测试/数值核心 |
| MATH-01 | P1 | 部分完成 | 持续维护命题—假设—代码—反例/测试映射 | 修改优化涉及假设时同步检查/文稿；明确精确恒等式与浮点求解的差别；不把测试数当证明 | 数学/数值核心 |
| MATH-02 | P2；主张原创时前置 | 需求待确认 | 第三方数学复核和历史原创性专题检索 | 分别形成正确性与新颖性意见；保留未认定原创的当前标记，直到可核对文献证据充分 | 研究/外部审阅 |

## D. 中长期候选，不属于轻量性能补丁

| ID | 优先级 | 当前状态 | 功能/原因 | 进入开发与验收条件 | 建议责任角色 |
|---|---|---|---|---|---|
| FUT-01 | P2 | 需求待确认 | 分块线性 IV 弱识别/首阶段诊断减少 dense materialization | 真实工作负载证明诊断占主导；保持 KP/SW 等完整语义与外部/独立对照；不能靠省略诊断提速 | 线性 IV |
| FUT-02 | P2 | 需求待确认 | extra-nullity 的稀疏零空间表示与可估函数支持 | 具体经济应用、参数化/识别规格和资源预算；不可识别值不得伪装为唯一系数 | 后估计/数学 |
| FUT-03 | P2 | 需求待确认 | FE 标准误、leave-out/AKM 校正、更多 bootstrap 族 | 每项独立设计文档、文献、数据/识别条件和参考验收；不统一混成“补齐推断” | 推断/研究 |
| FUT-04 | P2 | 按支持声明安排 | GPU/可选 IO/FLINT 或更多后端验证/扩展 | 有实际设备与依赖；已有公开支持与新后端开发分开；不因某后端未实测便宣称认证 | 后端/发布工程 |

## 每个任务的 issue 模板

```text
ID / 标题:
优先级 / 当前状态:
当前维护者与审阅者: 待指定
基线提交 / 制品哈希:
目标迭代: 待维护者确认
问题与已有证据:
允许改动范围 / 不允许改变的数值和公共契约:
依赖条件 / 阻塞原因:
正确性验收:
性能与内存验收（适用时）:
原始结果与环境位置:
回退方案:
完成提交 / 文档 / release:
```

## 通用合并标准

保持数值和统计契约，不放宽原断言；单项优化独立测量，不能只测多个补丁合计；记录所有重复计时和实际执行路径，profiling 单独运行；明显失败保留最小反例，不能以偶然正常样本掩盖。

性能合并阈值应在看候选结果前按目标机器噪声范围和重要工作负载确定，不设置未经校准的通用 CI 秒数门槛。新语义或字段按当前版本/兼容性政策处理；已发布制品不可覆盖。

不为速度默认降低精度、启用宽泛 fastmath、放松停止容差、删除 FE 正交性诊断、把过期权重投影复用为当前结果，或省略用户请求的统计推断。

## 建议顺序

REL-03、PERF-01、QA-01 已在当前候选落地；下一步：REL-01/02 实际外部验收 → PERF-02/03 按独立内存/速度证据决定 → PERF-04/05/06 测量及校准。REL-01/02/04 和按声明需要的 VAL-01 并行推进，不必等待所有 P1/P2 完成才发布。P2 只有真实需求和验收条件明确后才承诺。

## 0.6.3 本轮边界

仅改 `HDFEAbsorber._weighted_colsum` 的 NumPy 两条归约路径；PERF-02 投影缓冲区复用和 PERF-03/04/05/06 未合入。GPU 分支、QR/SVD、模型公式、公共参数、容差、迭代预算及原有测试断言不变。

REL-03 / QA-01 的完成表示门禁/启动器已实施并经本地测试，不表示 REL-01/02 自动通过。原定向测试和执行门禁测试分开统计，合成门禁证据不登记为真实跨平台结果。当前测试和性能实测见 `docs/development/test-status.md`、`docs/release/performance-0.6.3.md` 和交付证据包。
